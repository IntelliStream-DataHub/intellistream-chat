# Marketing site

`index.html` is a standalone, self-contained landing page for **threadorbit.com** — not part of
the Spring Boot build and not served by the app (the app's in-product landing page is
`src/main/resources/templates/landing.html`). Deploy it to any static host.
